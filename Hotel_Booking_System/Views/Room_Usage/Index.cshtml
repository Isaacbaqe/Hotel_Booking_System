@model IEnumerable<Hotel_Booking_System.Models.Room_Usage>

@{
    ViewBag.Title = "Booked Rooms";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Book New Room", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Guest_name
        </th>
        <th>
            Country
        </th>
        <th>
            City
        </th>
        <th>
            phone
        </th>
        <th>
            Room number
        </th>
        

    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Country)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.City)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Phone)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Resroom_number)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Booking_Name)
    </td>
    <td>
        @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
    </td>
</tr>
}

</table>
